# Public browser measurement IDs. These are build-time Vite variables, not secrets.
# Configure production values in Cloudflare Pages when the final IDs are available.
# Leave a value blank to keep that analytics provider fully disabled.
VITE_GA_MEASUREMENT_ID=
VITE_CLARITY_PROJECT_ID=
